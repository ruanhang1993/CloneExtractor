
需求分析结果：
1.克隆的类型只专注于Type-1和Type-2
2.根据不同的克隆场景来评估克隆代码的影响程度。
3.系统的输入是一个项目的两个不同版本的代码。
4.系统的输出是折算后的有效代码行数。
5.对历史版本中的克隆片段，在新版本中重构同时将历史克隆代码删除，工作量折算是历史克隆代码行数+新版本重构代码行数。（分场景）
6.工具根据克隆片段数目对克隆文件进行统计并以列表形式展示 。
7.系统输入的代码来源是SVN库同时要考虑CC库。
8.目前只考虑项目内克隆。
9.克隆侦测是以项目为单位。
10.数据库是mySql。 
11.系统是web版本，B/S结构即浏览器和服务器结构。
12.开发环境Tomcat+Linux（redhat==centos）

中汇需要配合：
1.现有工作量评定工具都有哪些？有哪些标准？ 
2两个版本的变更代码行数是否是我们系统要计算的，还是贵司有工具能计算出来作为输入供我们使用。

需要解决讨论的问题：
在克隆片段中插入没有意义的语句，将克隆片段拆分成小于5行，如何检查？

